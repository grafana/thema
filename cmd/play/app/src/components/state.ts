export interface State {
    lineage: string;
    input: string;
    output: string;
    share: string;
}

export const defaultState = {
    lineage: '',
    input: '',
    output: '',
    share: '',
}

