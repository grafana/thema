-- in.cue --

import "github.com/grafana/thema"

thema.#Lineage
name: "schemaless"
-- out/bindfail --
invalid lineage, #Lineage.schemas must be concrete
(1) forced error mark
  | "invalid lineage"
  | github.com/cockroachdb/errors/withstack/*withstack.withStack::
Wraps: (2) invalid lineage, #Lineage.schemas must be concrete
Error types: (1) *markers.withMark (2) *errors.errorString
