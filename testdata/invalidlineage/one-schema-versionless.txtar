-- in.cue --

import "github.com/grafana/thema"

thema.#Lineage
name: "one-schema-versionless"
schemas: [
	{
		schema: {
			firstfield: string
		}
	}
]
-- out/bindfail --
#SchemaDef.version must have concrete major and minor versions
(1) forced error mark
  | "invalid lineage"
  | github.com/cockroachdb/errors/withstack/*withstack.withStack::
Wraps: (2) #SchemaDef.version must have concrete major and minor versions
Error types: (1) *markers.withMark (2) *errors.errorString
